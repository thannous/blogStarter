'use strict';

describe('blog.viewContact module', function() {

  beforeEach(module('blog.viewContact'));

  describe('viewContact controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('ContactCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});