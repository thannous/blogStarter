'use strict';

describe('blog.viewListArticle module', function() {

  beforeEach(module('blog.viewListArticle'));

  describe('Article controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listArticleCtrl = $controller('ListArticleCtrl');
      expect(listArticleCtrl).toBeDefined();
    }));

  });
});